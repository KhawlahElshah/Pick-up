<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.projects">

    <uses-sdk android:minSdkVersion="12" android:targetSdkVersion="17"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Transport">

        <activity
            android:name=".LoginActivity"
            android:label="Main activity"
            android:configChanges="orientation|keyboardHidden|screenSize"> <!-- prevent main activity from restart when screen rotated -->

            <!-- set current activity as default activity to start App -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>


        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".DynamicActivity">

        </activity>

        <activity android:name=".SigninActivity"/>
        <activity android:name=".CatagoriesActivity">



            
        </activity>
        <activity android:name=".AddressInformationActivity"/>
        <activity android:name=".ControlsActivity">


        </activity>

        <activity android:name=".DeliveryInformationActivity"/>

    </application>

</manifest>
